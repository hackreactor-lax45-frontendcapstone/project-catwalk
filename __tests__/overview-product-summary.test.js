// import TestRenderer from 'react-test-renderer';
// import ProductDescription from '../client/src/components/Overview/ProductDescription.jsx';
// import ProductSummary from '../client/src/components/Overview/ProductSummary.jsx';

it('Confirms that Jest works', () => {
  expect(3).toEqual(3);
});

// it('Confirms product name', () => {
//   const testRenderer = TestRenderer.create(ProductSummary);
//   const testInstance = testRenderer.root;

//   expect(testInstance.findByProps({id: 'body-overview-name'}).children).toEqual(['Heir Force Ones']);
// });

// xit('Confirms product slogan', () => {
//   const testRenderer = TestRenderer.create(ProductDescription);
//   const testInstance = testRenderer.root;

//   expect(testInstance.findByProps({id: 'body-overview-slogan'}).children).toEqual(['A sneaker dynasty']);
// });

